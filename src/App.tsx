import MainPage from 'pages/main/MainPage';
import Layout from './shared/components/layout/Layout';

const App = () => {
  return (
    <Layout>
      <MainPage />
    </Layout>
  );
};

export default App;
